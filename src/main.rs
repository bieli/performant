fn run_and_measure(cmd: &str, args: &[&str]) -> (Duration, RUsage) {
    let start = Instant::now();
    let end = Instant::now();

    let usage = null;
    (end - start, usage)
}

fn main() {
  todo!("TODO: impl");
}
